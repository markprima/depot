development:
  adapter: sqlite3
  url: sqlite3://localhost:3000

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: depot_production
